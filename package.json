{
  "name": "billson-pro-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "server.js",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
