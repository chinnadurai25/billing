<IfModule mod_rewrite.c>
  RewriteEngine On

  # Rewrite root traffic to frontend/dist folder for Hostinger deployment
  RewriteCond %{REQUEST_URI} !^/frontend/dist/
  RewriteRule ^(.*)$ frontend/dist/$1 [L]
</IfModule>
